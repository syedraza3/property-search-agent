name: Property Search

on:
  schedule:
    # Every day at 9pm UK time
    - cron: "0 21 * * *"
  workflow_dispatch:  # also allows manual trigger from GitHub UI

jobs:
  search:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      # ── Restore sent listings cache ──────────────────────────────────────
      - name: Restore sent listings cache
        uses: actions/cache@v6
        with:
          path: sent_listings.json
          key: sent-listings-${{ runner.os }}
          restore-keys: |
            sent-listings-

      # ── Python setup ─────────────────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      # ── Run the agent ────────────────────────────────────────────────────
      - name: Run property search agent
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: python agent.py

      # ── Save updated sent listings cache ─────────────────────────────────
      - name: Save sent listings cache
        uses: actions/cache/save@v6
        if: always()
        with:
          path: sent_listings.json
          key: sent-listings-${{ runner.os }}-${{ github.run_id }}
